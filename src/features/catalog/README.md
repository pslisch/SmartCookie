# Catalog Feature

This feature module governs all course catalog browsing views, search bar query filtering, and category track stream cards.

## Directory Structure

```
catalog/
├── pages/
│   └── Catalog.tsx   # Catalog search, filtering, and study tracks
└── README.md
```

## Dependencies
- `lucide-react`
- `motion` (animations)
