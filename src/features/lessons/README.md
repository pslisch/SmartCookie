# Lessons Feature

This feature module governs all student dashboard views, lesson player contexts, student course enrollment indexes, and streak tracking widgets.

## Directory Structure

```
lessons/
├── pages/
│   └── MyLessons.tsx   # Enrolled course dashboard and statistics
└── README.md
```

## Dependencies
- `lucide-react`
- `motion` (animations)
- `shared/types` (Tab state definitions)
